name: group_s
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.0 # State management (from Units 3-4)
  supabase_flutter: ^2.0.0 # Supabase SDK
  image_picker: ^1.0.5 # Pick images from device
  google_fonts: ^6.2.1  # Beautiful fonts
  confetti: ^0.7.0 # Confetti animation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
